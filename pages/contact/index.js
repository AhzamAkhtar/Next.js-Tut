//  File-system based routing mechanism
// When a file is a added to the 
//pages folder in a project,
// by default all the files becomes 
// avaliable as a route

// You Should always name component => index.js
//in folder


const index = () => {
  return (
    <div>
        <h1>
            Contact Component
        </h1>
    </div>
  )
}

export default index